# 📌 **Fitness Pose AI — 모델링 & 전처리 개요**
### **작성자: 윤수진**
---

본 문서는 **운동 분류(Stage 1)** 및 **자세 교정(Stage 2)** 모델 개발을 위해 수행한  
**데이터 전처리 과정과 전체 모델 구조**를 정리한 문서

업로드된 전처리 스크립트  
`train_preprocessing_final.py`, `validation_preprocessing_final.py`, `checking_preprocessing.py`  
는 **로컬 기준 절대경로로 작성**되어 있으며,  
사용 시 **각자 환경에 맞는 경로로 수정 후 실행**


## 📌 1. 모델링 구조

모델은 다음과 같은 2단계 파이프라인으로 구성

---

### Stage 1 — 운동 분류 모델 (Classification)

- 입력: **256×256 이미지**
- 출력: **운동 클래스** (예: squat, lunge, burpee 등)
- 목적: “이 프레임이 어떤 운동인지” 분류
- 특징: keypoints 사용 X (순수 CNN 기반 이미지 분류 모델)
- 모델 타입: CNN 이미지 기반 분류

---

### Stage 2 — 자세 교정 하이브리드 모델 (Pose Correction)

- 입력:
  - **256×256 이미지**
  - **keypoints (pts)**
- 출력: **운동별 컨디션(pass/fail)**
- 특징:
  - CNN 이미지 feature + keypoint feature  
  - 운동별 규칙 기반 condition 평가

---

# 📌 2. 전처리 흐름 (자동화 설계)

데이터가 매우 크기 때문에 **body 단위(batch 단위)** 로 처리

전처리 스크립트 기능:

- 이미지 256×256 리사이즈
- Stage1 / Stage2 각각 필요한 형태로 데이터 생성
- keypoints, conditions 자동 포함
- source(body_xx) 정보 자동 기록
- 이미 존재하는 파일 중복 방지 (skip)
- 이미지 번호 자동 증가
- train/valid 자동 분리 지원

---

# 📌 3. 전처리 출력 구조

```bash
fitness_dataset/
├─ stage1_classification/
│   ├─ train/images
│   ├─ train/labels
│   ├─ valid/images
│   └─ valid/labels
│
└─ stage2_pose_correction/
    ├─ train/images
    ├─ train/labels
    ├─ valid/images
    └─ valid/labels
```

---

# 📌 4. 라벨 구조

### Stage1 (운동 분류용 라벨)

```json
{
  "image": "train_0001234.jpg",
  "exercise": "squat",
  "source": "body_03"
}
```

### Stage2 (자세 교정용 라벨)

```json
{
  "image": "train_0001234.jpg",
  "exercise": "squat",
  "pose": "down",
  "conditions": { ... },
  "keypoints": { ... },
  "active": "Yes",
  "source": "body_03"
}
```

# 📌 5. 모델 학습 흐름

1. Stage1 
 - 이미지 기반 운동 분류 모델 학습
2. 운동 종류 예측값 기반 Stage2 선택
 - 예: Stage1 → squat → squat 전용 자세교정 모델로 전달
3. Stage2
 - 이미지 + keypoints 입력
 - 운동별 조건 검증(pass/fail)


# 📌 6. 데이터셋 출처 (Dataset Source)
본 프로젝트는 AI Hub 피트니스 자세 데이터셋(AI Hub – Fitness Pose Dataset) 을 기반으로 함
- 출처: https://www.aihub.or.kr/aihubdata/data/view.do?currMenu=115&topMenu=100&dataSetSn=231
- 데이터 구성: 운동 영상, 프레임 이미지, 포즈(keypoints), 자세 조건(conditions)
- 사용 목적: 운동 분류 및 자세 교정 모델 학습 및 추론용 데이터 구축


# 📌 7. 개발 환경 (Development Environment)
본 프로젝트의 전처리 및 모델 개발은 아래 환경에서 진행

### 🔧 Python 버전
- Python 3.10

### 🔧 주요 패키지 버전
(전처리 스크립트 실행 시 사용한 핵심 패키지)
- opencv-python
- numpy
- pillow (PIL)
- tqdm
- json / os / re (Python built-ins)

자신의 로컬에 맞게 경로만 변경하여 실행